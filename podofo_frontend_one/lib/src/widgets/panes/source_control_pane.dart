import 'package:flutter/material.dart';

class SourceControlPane extends StatelessWidget {
  const SourceControlPane({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Source Control'));
  }
}
