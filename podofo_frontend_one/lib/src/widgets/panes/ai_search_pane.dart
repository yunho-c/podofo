import 'package:flutter/material.dart';

class AISearchPane extends StatelessWidget {
  const AISearchPane({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('AI Search'));
  }
}
