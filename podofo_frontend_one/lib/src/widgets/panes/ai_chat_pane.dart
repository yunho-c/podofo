import 'package:flutter/material.dart';

class AIChatPane extends StatelessWidget {
  const AIChatPane({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('AI Chat'));
  }
}
