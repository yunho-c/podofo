import 'package:flutter/material.dart';

class AnnotationPane extends StatelessWidget {
  const AnnotationPane({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Annotation'));
  }
}
